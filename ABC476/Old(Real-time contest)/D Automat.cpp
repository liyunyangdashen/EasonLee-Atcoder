#include<iostream>
using namespace std; using ll = long long; const int N = 2e5 + 5;
ll n,m,k,x,y,a[N],b[N];
int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  cin >> n >> m >> k >> x >> y;
  for (int i = 1;i <= n;i++)
    cin >> a[i];
  for (int j = 1;j <= m;j++)
    cin >> a[j];
  
  return 0;
}
